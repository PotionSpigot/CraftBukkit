package net.minecraft.server;

import java.util.concurrent.Callable;

















































































































































































































































class CrashReportModded
  implements Callable
{
  CrashReportModded(DedicatedServer paramDedicatedServer) {}
  
  public String a()
  {
    String str = this.a.getServerModName();
    if (!str.equals("vanilla")) return "Definitely; Server brand changed to '" + str + "'";
    return "Unknown (can't tell)";
  }
}
