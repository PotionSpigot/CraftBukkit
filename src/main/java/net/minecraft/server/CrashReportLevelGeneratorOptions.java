package net.minecraft.server;

import java.util.concurrent.Callable;































































































































































































































































































































































































class CrashReportLevelGeneratorOptions
  implements Callable
{
  CrashReportLevelGeneratorOptions(WorldData paramWorldData) {}
  
  public String a()
  {
    return WorldData.c(this.a);
  }
}
